# Отслеживание Переходов по Реферальным Ссылкам

## 🔗 Как работает счетчик "ПЕРЕХОДОВ по ссылке"

### Счетчик учитывает ДВА типа событий:

---

## 1️⃣ **Переходы ПО ССЫЛКЕ и QR-коду** (входящие)

### Когда увеличивается:
Когда кто-то переходит по вашей реферальной ссылке или сканирует QR-код

### Как это работает:

**QR-код содержит:**
```
https://t.me/Jpuffbot?start=USER_ID
```

**Ссылка содержит тоже самое:**
```
https://t.me/Jpuffbot?start=USER_ID
```

**При переходе:**
1. Открывается Telegram бот `@Jpuffbot`
2. Параметр `start=USER_ID` передается в приложение
3. `referral-handler.js` обрабатывает параметр
4. Вызывается `trackReferralClick(USER_ID)` из bonus-system.js
5. Увеличивается счетчик `linkClicks` у владельца ссылки

**Код в `referral-handler.js`:**
```javascript
function handleReferralCode(referrerId) {
    // ... 
    trackReferralClick(referrerId); // Увеличивает счетчик
    // ...
}
```

---

## 2️⃣ **Действия пользователя СО ССЫЛКОЙ** (исходящие)

### Когда увеличивается:
Когда ВЫ копируете или делитесь своей реферальной ссылкой

### Отслеживаемые действия:

**1. Кнопка "Копировать" (📋):**
```javascript
window.copyReferralLink = async () => {
    // ... копирование ...
    trackLinkShare(userId); // +1 к счетчику
}
```

**2. Кнопка "Поделиться" (🔗):**
```javascript
window.shareNative = async () => {
    // ... шаринг ...
    trackLinkShare(userId); // +1 к счетчику
}
```

**3. Клик по QR-коду для увеличения:**
- Не учитывается (это просто просмотр)

---

## 📊 Итоговая статистика "ПЕРЕХОДОВ":

### Счетчик показывает:
- ✅ Клики по кнопке "Копировать"
- ✅ Клики по кнопке "Поделиться"
- ✅ Реальные переходы других пользователей (когда кто-то открывает бота по вашей ссылке/QR)

### Формула:
```
ПЕРЕХОДОВ = (копирования + шаринги) + реальные переходы
```

---

## 💾 Хранение:

### localStorage ключ:
```javascript
juicy_referral_stats_{userId}
```

### Структура:
```json
{
  "linkClicks": 15  // Общий счетчик всех действий
}
```

---

## 🔄 Обновление UI:

### Мгновенное обновление:
При копировании/шаринге счетчик обновляется сразу:
```javascript
function trackLinkShare(userId) {
    // ... обновить данные ...
    
    // Обновить UI сразу
    const clicksEl = document.getElementById('link-clicks');
    if (clicksEl) {
        clicksEl.textContent = stats.linkClicks;
    }
}
```

---

## ✅ Что реализовано:

- [x] Отслеживание копирования ссылки
- [x] Отслеживание шаринга ссылки
- [x] Отслеживание реальных переходов (через referral-handler)
- [x] Отображение в UI на странице referral.html
- [x] Мгновенное обновление счетчика
- [x] Сохранение в localStorage

---

## 🎯 Резюме:

**Счетчик "ПЕРЕХОДОВ по ссылке"** = это комбинированный показатель, который показывает:
1. Сколько раз вы поделились своей ссылкой
2. Сколько людей реально перешло по ней

Это дает полную картину активности вашей реферальной программы! 📈
