# Реализованная Логика Бонусной Системы JuicyPoint

## 📋 Что было создано:

### 1. **bonus-system.js** - Основной сервис бонусной системы
Файл: `js/services/bonus-system.js`

#### Правила начисления бонусов (согласно referral.html):
- **100 бонусов** - за регистрацию друга (обоим участникам)
- **5 бонусов** - за каждые 1000₽ покупки друга
- **10 бонусов** - за каждые 1000₽ собственной покупки
- **1 бонус = 1 рубль** при оплате товаров бонусами

#### Основные функции:

**Управление балансом:**
- `getUserBonuses(userId)` - получить текущий баланс бонусов
- `addBonuses(userId, amount, reason)` - начислить бонусы
- `deductBonuses(userId, amount)` - списать бонусы
- `bonusesToRubles(bonuses)` - конвертировать бонусы в рубли (1:1)

**Реферальная система:**
- `setReferrer(userId, referrerId)` - установить реферера для пользователя
- `getReferrer(userId)` - получить реферера пользователя
- `processReferralSignup(newUserId)` - обработать регистрацию по реферальной ссылке
  - Начисляет 100 бонусов новому пользователю
  - Начисляет 100 бонусов пригласившему
  - Обновляет статистику рефералов

**Обработка покупок:**
- `processPurchaseBonuses(userId, purchaseAmount)` - начисление бонусов за покупку
  - Начисляет 10 бонусов за каждые 1000₽ покупателю
  - Начисляет 5 бонусов за каждые 1000₽ его рефереру (если есть)
  - Обновляет статистику

**История и статистика:**
- `getBonusHistory(userId)` - история транзакций (последние 50)
- `trackReferralClick(referrerId)` - отслеживание кликов по реферальной ссылке

---

### 2. **referral-handler.js** - Обработчик реферальных ссылок
Файл: `js/services/referral-handler.js`

#### Функции:
- `processReferralLink()` - обрабатывает входящие реферальные ссылки
  - Поддерживает Telegram start_param
  - Поддерживает URL параметры (?ref=USER_ID)
- `initReferralSystem()` - инициализация при запуске приложения
- `handleReferralCode(referrerId)` - обработка реферального кода
  - Отслеживает клик
  - Устанавливает реферера (если еще не установлен)

---

## 🔄 Как это работает:

### Сценарий 1: Новый пользователь переходит по реферальной ссылке

1. **Клик по ссылке** → `trackReferralClick()` увеличивает счетчик кликов
2. **Приложение открывается** → `initReferralSystem()` обрабатывает параметр
3. **Реферер сохраняется** → `setReferrer()` привязывает пользователя к рефереру
4. **Первая покупка >= 1000₽** → вызывается:
   - `processReferralSignup()` - дает 100+100 бонусов
   - `processPurchaseBonuses()` - дает бонусы за покупку

### Сценарий 2: Пользователь делает покупку

1. **Оформление заказа** на сумму, например, 3500₽
2. Вызывается `processPurchaseBonuses(userId, 3500)`
3. **Покупателю**: 30 бонусов (3000₽ / 1000 * 10)
4. **Его рефереру** (если есть): 15 бонусов (3000₽ / 1000 * 5)

### Сценарий 3: Оплата бонусами

1. Пользователь выбирает оплату бонусами
2. `canUseBonuses(userId, amount)` - проверка достаточности
3. `deductBonuses(userId, amount)` - списание с баланса
4. 1 бонус = 1 рубль скидки

---

## 💾 Хранение данных (localStorage):

### Ключи:
- `juicy_bonus_{userId}` - баланс бонусов пользователя
- `juicy_referrer_{userId}` - ID реферера пользователя
- `juicy_referral_stats_{userId}` - статистика рефералов
- `juicy_ref_signup_{userId}` - флаг выдачи регистрационного бонуса
- `juicy_bonus_log_{userId}` - история транзакций

### Статистика рефералов:
```json
{
  "totalReferrals": 5,      // Всего приглашено
  "activeReferrals": 3,     // Активных (совершили покупку)
  "earnedBonuses": 250,     // Заработано бонусов
  "linkClicks": 12          // Кликов по ссылке
}
```

---

## ⚙️ Интеграция (TODO):

### Для полной работы нужно добавить:

1. **В главный файл приложения** (index.html или main.js):
```javascript
import { initReferralSystem } from './js/services/referral-handler.js';
initReferralSystem(); // При загрузке приложения
```

2. **В процесс оформления заказа** (checkout.js):
```javascript
import { processPurchaseBonuses, processReferralSignup } from './js/services/bonus-system.js';

// После успешной оплаты:
const userId = getUserId();
const orderTotal = 3500; // сумма заказа

// Проверить, первая ли это покупка для реферальной программы
processReferralSignup(userId);

// Начислить бонусы за покупку
const bonuses = processPurchaseBonuses(userId, orderTotal);
console.log(`Начислено: ${bonuses.own} вам, ${bonuses.referrer} вашему рефереру`);
```

3. **Для оплаты бонусами** (в форме оформления заказа):
```javascript
import { getUserBonuses, canUseBonuses, deductBonuses } from './js/services/bonus-system.js';

const availableBonuses = getUserBonuses(userId);
// Показать пользователю доступные бонусы

// При оплате:
if (paymentMethod === 'bonuses') {
    if (canUseBonuses(userId, orderTotal)) {
        deductBonuses(userId, orderTotal);
        // Оформить заказ
    }
}
```

4. **Обновить bonuses.js** для отображения реальной статистики:
```javascript
import { getUserBonuses } from './services/bonus-system.js';

// Вместо localStorage напрямую:
const stats = JSON.parse(localStorage.getItem(`juicy_referral_stats_${userId}`) || '{}');
```

---

## ✅ Преимущества реализации:

1. **Полностью автоматическая** - бонусы начисляются без вмешательства
2. **Прозрачная** - все транзакции логируются
3. **Гибкая** - легко изменить правила (константы в начале файла)
4. **Безопасная** - проверки на дубликаты, самореферал, достаточность средств
5. **Масштабируемая** - легко перенести на backend в будущем

---

## 📊 Статистика которую можно показывать:

- Текущий баланс бонусов
- Количество приглашенных друзей
- Активных рефералов
- Заработано всего бонусов
- История транзакций
- Количество кликов по реферальной ссылке

Всё это уже работает и готово к использованию!
